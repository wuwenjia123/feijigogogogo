# 飞机大战网页游戏 - 技术设计文档

> 版本: v1.0  
> 文档类型: 技术设计/开发指南  
> 目标读者: Solo Builder / 全栈开发者  
> 技术栈: HTML5 Canvas + Vanilla JavaScript + CSS3

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术选型](#2-技术选型)
3. [游戏核心玩法设计](#3-游戏核心玩法设计)
4. [系统架构与模块划分](#4-系统架构与模块划分)
5. [核心数据结构](#5-核心数据结构)
6. [游戏核心系统详解](#6-游戏核心系统详解)
7. [UI/UX 设计规范](#7-uiux-设计规范)
8. [关卡与进度设计](#8-关卡与进度设计)
9. [性能优化方案](#9-性能优化方案)
10. [实现步骤与里程碑](#10-实现步骤与里程碑)
11. [测试验收标准](#11-测试验收标准)

---

## 1. 项目概述

### 1.1 项目简介

一款基于 HTML5 Canvas 的纵向卷轴射击游戏（STG），融合市面热门飞机大战玩法，主打**科技赛博朋克风格**，提供丰富的武器系统、多样化敌机、巨型 BOSS 战、弹幕躲避等核心体验。

### 1.2 设计目标

| 目标 | 描述 |
|------|------|
| **视觉表现** | 科技霓虹风格，粒子特效丰富，画面冲击力强 |
| **玩法深度** | 5+武器类型、4+敌机种类、多阶段 BOSS、道具系统 |
| **操作手感** | 60FPS 流畅体验，触控/鼠标双支持，打击感强 |
| **扩展性** | 模块化架构，便于后续新增关卡、BOSS、武器 |
| **兼容性** | 支持主流浏览器（Chrome/Safari/Firefox/Edge） |

### 1.3 参考作品

- **雷电系列**：经典纵向卷轴射击的标杆
- **全民飞机大战**：宠物/装备养成、社交化玩法
- **东方 Project**：密集弹幕、精准碰撞
- **傲气雄鹰 (Sky Force)**：精美 2.5D 视觉、升级系统

---

## 2. 技术选型

### 2.1 技术栈

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 渲染 | HTML5 Canvas 2D | 原生高性能，适合 2D 粒子/弹幕游戏 |
| 语言 | JavaScript (ES6+) | 无需编译，浏览器原生支持 |
| 样式 | CSS3 + CSS Variables | UI 层样式管理，支持主题切换 |
| 音效 | Web Audio API | 合成音效 + 音频缓冲，低延迟 |
| 存储 | localStorage | 本地存档、最高分记录 |
| 构建 | 无（单文件或多文件直引） | 降低 Solo Builder 复杂度 |

### 2.2 为什么不用框架？

- **性能可控**：游戏主循环需要精细控制，框架抽象层会增加开销
- **上手简单**：Solo Builder 可直接阅读源码，无需学习框架 API
- **打包零成本**：纯静态文件，双击 HTML 即可运行
- **调试方便**：Chrome DevTools 直接调试 Canvas 和 JS

### 2.3 核心性能指标

- **目标帧率**：60 FPS（桌面端）/ 30 FPS（移动端）
- **单帧预算**：≤ 16.6ms
- **内存占用**：≤ 200MB
- **首屏加载**：≤ 2s（无资源预加载场景）

---

## 3. 游戏核心玩法设计

### 3.1 游戏流程

```
主菜单 → 选择战机 → 关卡选择 → 游戏进行 → BOSS战 → 结算界面
    ↑                                           ↓
    └───────────────────────────────────────────┘
```

### 3.2 操作方式

| 操作 | 桌面端 | 移动端 |
|------|--------|--------|
| 移动 | 鼠标拖动 / WASD / 方向键 | 触屏拖动 |
| 射击 | 自动射击（默认） | 自动射击（默认） |
| 释放技能/炸弹 | 空格 / 鼠标左键双击 | 双指点击 / 技能按钮 |
| 暂停 | ESC / P | 暂停按钮 |

### 3.3 核心玩法系统

#### 3.3.1 武器系统（5种武器，可切换）

| 武器 | 特点 | 升级效果 | 适用场景 |
|------|------|----------|----------|
| **激光炮 (Laser)** | 单发高伤害，穿透性强 | 伤害+、射速+、可多联装 | BOSS战、精英怪 |
| **散弹 (Spread)** | 扇形多发，覆盖广 | 弹数+、角度+、伤害+ | 清小兵、弹幕压制 |
| **导弹 (Missile)** | 追踪目标，爆炸伤害 | 追踪速度+、爆炸范围+ | 复杂地形、移动靶 |
| **等离子环 (Plasma)** | 环绕护体，接触伤害 | 数量+、半径+、伤害+ | 防御、近战 |
| **磁暴波 (EMP)** | 范围技能，清屏+减速 | 范围+、冷却-、伤害+ | 危急时刻、清弹幕 |

**切换机制**：拾取武器道具切换，或按数字键 1-5 切换已解锁武器。

#### 3.3.2 道具系统

| 道具 | 图标颜色 | 效果 | 持续时间 |
|------|----------|------|----------|
| 武器升级 (PowerUp) | 黄色 P | 当前武器等级+1 | 永久（本局） |
| 护盾 (Shield) | 蓝色 S | 免疫一次伤害 | 直到被击破 |
| 炸弹 (Bomb) | 红色 B | 清屏+全屏伤害 | 即时 |
| 回血 (Heal) | 绿色 H | 恢复 1 点生命 | 即时 |
| 分数加成 (Score) | 金色 $ | 分数 x2 | 10秒 |
| 武器切换 | 彩色 W | 随机切换武器 | 永久（本局） |

#### 3.3.3 敌机类型

| 敌机 | 血量 | 移动模式 | 攻击方式 | 分数 |
|------|------|----------|----------|------|
| **小型机 (Scout)** | 1 | 直线 / 正弦曲线 | 偶尔单发 | 100 |
| **中型机 (Fighter)** | 3 | 追踪 / 编队 | 连发 / 扇形 | 300 |
| **重型机 (Bomber)** | 8 | 缓慢直线 | 散射炸弹 | 800 |
| **精英机 (Elite)** | 15 | 复杂机动 | 多种弹幕 | 2000 |
| **BOSS** | 分阶段 | 多模式 | 弹幕密集 | 5000-20000 |

#### 3.3.4 BOSS 战设计

**BOSS 通用规则**：
- 每大关末尾出现，血量分多阶段
- 每阶段血条下降到阈值时，切换攻击模式
- 阶段切换时出现硬直 + 武器升级道具掉落
- BOSS 背景变化 + 音乐切换

**示例 BOSS：「虚空母舰」- 第 1 关 BOSS**

| 阶段 | 血量占比 | 攻击模式 | 特点 |
|------|----------|----------|------|
| 第一阶段 | 100%~70% | 扇形弹幕 + 小型机召唤 | 移动缓慢，弹幕稀疏 |
| 第二阶段 | 70%~40% | 旋转弹幕 + 激光扫射 | 弹幕密度提升 |
| 第三阶段 | 40%~0% | 全屏弹幕 + 追踪弹 + 冲撞 | 狂暴模式，极高密度 |

#### 3.3.5 弹幕系统

**弹幕类型**：
1. **直线弹**：最基础，直线飞行
2. **扇形弹**：多发呈扇形展开
3. **旋转弹**：围绕发射点旋转射出
4. **追踪弹**：缓慢追踪玩家
5. **激光束**：预警线 → 实弹伤害
6. **环形弹**：360° 全向发射
7. **随机弹**：不规则运动轨迹

**碰撞判定**：
- 玩家「判定点」大小：3x3 像素（位于飞机中心）
- 敌弹判定：按实际贴图缩小 30%
- 体现「擦弹」快感（擦弹有加分/能量累积）

---

## 4. 系统架构与模块划分

### 4.1 整体架构图

```
┌───────────────────────────────────────────────────┐
│                   Game Engine                     │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │
│  │  GameLoop   │  │ InputMgr    │  │ AudioMgr  │ │
│  └─────────────┘  └─────────────┘  └───────────┘ │
├───────────────────────────────────────────────────┤
│                   Game Systems                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ Player   │ │ EnemyMgr │ │ BulletMgr│          │
│  └──────────┘ └──────────┘ └──────────┘          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ PowerUp  │ │ Particle │ │ Collision│          │
│  └──────────┘ └──────────┘ └──────────┘          │
│  ┌──────────┐ ┌──────────┐                       │
│  │  Boss    │ │  Level   │                       │
│  └──────────┘ └──────────┘                       │
├───────────────────────────────────────────────────┤
│                   Render Layer                    │
│  ┌─────────────┐  ┌──────────────────────────┐   │
│  │  Canvas     │  │  UI Layer (HTML/CSS)     │   │
│  │  Renderer   │  │  HUD / Menus / Dialogs   │   │
│  └─────────────┘  └──────────────────────────┘   │
└───────────────────────────────────────────────────┘
```

### 4.2 模块职责

| 模块 | 文件名 | 职责 |
|------|--------|------|
| **主入口** | `main.js` | 初始化游戏、启动主循环 |
| **游戏核心** | `game.js` | 游戏状态管理、主循环调度 |
| **玩家** | `player.js` | 玩家飞机移动、射击、生命值 |
| **敌机管理** | `enemy.js` | 敌机生成、AI、死亡判定 |
| **BOSS** | `boss.js` | BOSS 多阶段、弹幕模式 |
| **子弹管理** | `bullet.js` | 玩家/敌方子弹池、移动、碰撞 |
| **道具系统** | `powerup.js` | 道具生成、拾取、效果 |
| **粒子系统** | `particle.js` | 爆炸、拖尾、特效 |
| **碰撞检测** | `collision.js` | 各类碰撞判定 |
| **输入管理** | `input.js` | 鼠标/键盘/触屏输入 |
| **音频管理** | `audio.js` | 音效、背景音乐 |
| **关卡管理** | `level.js` | 关卡数据、敌潮编排 |
| **渲染器** | `renderer.js` | Canvas 绘制、相机、背景滚动 |
| **UI层** | `ui.js` | HUD、菜单、弹窗 |
| **工具类** | `utils.js` | 数学函数、对象池、随机 |

### 4.3 游戏状态机

```
MENU → PLAYER_SELECT → LEVEL_SELECT → PLAYING → PAUSED → PLAYING
                                              ↓
                                           GAME_OVER
                                              ↓
                                        RESULT_SCREEN
                                              ↓
                                            MENU
```

| 状态 | 触发条件 |
|------|----------|
| `MENU` | 初始状态 / 结算返回 |
| `PLAYING` | 开始游戏 |
| `PAUSED` | 按 ESC / 暂停按钮 |
| `GAME_OVER` | 生命耗尽 |
| `LEVEL_COMPLETE` | BOSS 被击败 |

---

## 5. 核心数据结构

### 5.1 游戏全局状态

```javascript
const GameState = {
  status: 'MENU',              // MENU / PLAYING / PAUSED / GAME_OVER / VICTORY
  score: 0,                    // 当前分数
  highScore: 0,                // 历史最高分（localStorage）
  lives: 3,                    // 剩余生命
  bombs: 2,                    // 炸弹数量
  currentLevel: 1,             // 当前关卡
  difficulty: 'NORMAL',        // EASY / NORMAL / HARD / INSANE
  combo: 0,                    // 连击数
  comboTimer: 0,               // 连击计时器
  screenShake: 0,              // 屏幕震动强度
  slowMotion: 1,               // 慢动作倍率（1=正常）
  time: 0                      // 游戏内时间（毫秒）
};
```

### 5.2 玩家数据结构

```javascript
class Player {
  x = 0;                       // 位置 X
  y = 0;                       // 位置 Y
  width = 48;                  // 宽度
  height = 64;                 // 高度
  speed = 6;                   // 移动速度
  hitboxRadius = 3;            // 判定点半径
  currentWeapon = 'LASER';     // 当前武器类型
  weaponLevel = 1;             // 武器等级 (1-5)
  hp = 3;                      // 生命值
  maxHp = 3;                   // 最大生命
  shield = false;              // 护盾状态
  shieldTimer = 0;             // 护盾剩余时间
  invincible = false;          // 无敌状态（被击后）
  invincibleTimer = 0;         // 无敌时间
  fireCooldown = 0;            // 射击冷却
  skillCooldown = 0;           // 技能冷却
  charge = 0;                  // 必杀能量（0-100）
}
```

### 5.3 子弹数据结构

```javascript
class Bullet {
  x = 0;
  y = 0;
  vx = 0;                      // X 方向速度
  vy = 0;                      // Y 方向速度
  radius = 4;                  // 碰撞半径
  damage = 1;                  // 伤害
  color = '#00ffff';           // 颜色
  type = 'NORMAL';             // NORMAL / LASER / MISSILE / PLASMA
  isPlayer = true;             // 玩家子弹 or 敌方子弹
  life = 0;                    // 存活时间
  maxLife = 5000;              // 最大存活时间
  target = null;               // 追踪目标（导弹用）
  pierce = 0;                  // 穿透次数
}
```

### 5.4 敌机数据结构

```javascript
class Enemy {
  x = 0;
  y = 0;
  width = 40;
  height = 40;
  hp = 1;
  maxHp = 1;
  speed = 2;
  type = 'SCOUT';              // SCOUT / FIGHTER / BOMBER / ELITE
  score = 100;
  fireCooldown = 0;
  fireRate = 1500;             // 射击间隔（毫秒）
  pattern = 'straight';        // 移动模式
  patternData = {};            // 模式参数
  dropRate = 0.1;              // 道具掉落率
  angle = 0;                   // 当前角度
  entryTime = 0;               // 出场时间
}
```

### 5.5 BOSS 数据结构

```javascript
class Boss {
  x = 0;
  y = 0;
  width = 200;
  height = 150;
  hp = 1000;
  maxHp = 1000;
  currentPhase = 0;            // 当前阶段
  phases = [];                 // 阶段配置数组
  phaseTimer = 0;              // 阶段计时器
  patterns = [];               // 当前阶段弹幕模式
  patternIndex = 0;            // 当前弹幕模式索引
  patternTimer = 0;            // 模式持续时间
  isEnraged = false;           // 是否狂暴
  entryAnimation = true;       // 入场动画
  xTarget = 0;                 // 目标位置 X
  yTarget = 0;                 // 目标位置 Y
}
```

### 5.6 粒子数据结构

```javascript
class Particle {
  x = 0;
  y = 0;
  vx = 0;
  vy = 0;
  life = 1000;                 // 存活时间（毫秒）
  maxLife = 1000;
  size = 3;                    // 大小
  color = '#ffffff';
  alpha = 1;                   // 透明度
  blendMode = 'lighter';       // 混合模式
  gravity = 0;                 // 重力
  shrink = true;               // 是否逐渐缩小
}
```

---

## 6. 游戏核心系统详解

### 6.1 游戏主循环

```
每帧执行顺序：
1. 计算 deltaTime（上一帧到当前帧的时间差）
2. 处理输入（Input.update）
3. 更新游戏逻辑：
   3.1 玩家更新（移动、射击冷却）
   3.2 子弹更新（位置、生命周期）
   3.3 敌机更新（AI、射击）
   3.4 BOSS 更新（阶段、弹幕）
   3.5 道具更新（下落、拾取）
   3.6 粒子更新（物理、衰减）
   3.7 碰撞检测（所有对象）
   3.8 关卡进度（敌潮生成、BOSS触发）
4. 渲染（按层级绘制）
5. 等待下一帧（requestAnimationFrame）
```

**关键代码模式**：

```javascript
class Game {
  lastTime = 0;
  
  loop(currentTime) {
    const deltaTime = currentTime - this.lastTime;
    this.lastTime = currentTime;
    
    // 限制最大帧间隔，防止切换标签后跳帧
    const dt = Math.min(deltaTime, 50);
    
    if (GameState.status === 'PLAYING') {
      this.update(dt);
    }
    
    this.render();
    requestAnimationFrame((t) => this.loop(t));
  }
  
  update(dt) {
    this.player.update(dt);
    this.bulletMgr.update(dt);
    this.enemyMgr.update(dt);
    this.powerupMgr.update(dt);
    this.particleMgr.update(dt);
    this.collision.check();
    this.level.update(dt);
    this.ui.update(dt);
  }
}
```

### 6.2 对象池模式（Object Pooling）

**为什么需要对象池？**
- 频繁创建/销毁子弹、粒子会触发 GC 卡顿
- 预分配对象池，复用已有对象，显著提升性能

**实现要点**：

```javascript
class ObjectPool {
  constructor(createFn, initialSize = 50) {
    this.createFn = createFn;
    this.pool = [];
    this.active = [];
    
    for (let i = 0; i < initialSize; i++) {
      this.pool.push(this.createFn());
    }
  }
  
  get() {
    const obj = this.pool.pop() || this.createFn();
    obj.active = true;
    this.active.push(obj);
    return obj;
  }
  
  release(obj) {
    obj.active = false;
    const idx = this.active.indexOf(obj);
    if (idx > -1) this.active.splice(idx, 1);
    this.pool.push(obj);
  }
  
  update(dt, updateFn) {
    for (let i = this.active.length - 1; i >= 0; i--) {
      const obj = this.active[i];
      updateFn(obj, dt);
      if (obj.life <= 0) this.release(obj);
    }
  }
}
```

**推荐池大小**：
- 玩家子弹池：100
- 敌方子弹池：300
- 粒子池：500
- 敌机池：50
- 道具池：20

### 6.3 碰撞检测系统

#### 6.3.1 碰撞类型与判定方式

| 碰撞对 | 判定方式 | 说明 |
|--------|----------|------|
| 玩家子弹 vs 敌机 | 圆形 vs 矩形/圆形 | 子弹用圆形，敌机用矩形或圆形 |
| 敌方子弹 vs 玩家 | 圆形 vs 圆形（判定点） | 玩家判定点 3px，敌弹按类型 |
| 敌机 vs 玩家 | 矩形 vs 圆形 | 玩家撞击敌机掉血 |
| 道具 vs 玩家 | 圆形 vs 圆形 | 拾取范围 30px（有磁吸） |

#### 6.3.2 空间划分优化

使用**网格划分（Spatial Grid）**减少碰撞检测次数：

```
将画布分成 N x N 的网格，每格大小 = 最大敌机宽度
只有同格或相邻格的对象才需要检测碰撞
时间复杂度从 O(n*m) 降到 O(n+m)
```

### 6.4 武器系统详解

#### 6.4.1 激光炮 (Laser)

```
等级1: 1条中央激光，伤害10
等级2: 1条激光，伤害15，射速+
等级3: 3条激光（左中右），伤害各10
等级4: 5条扇形激光
等级5: 5条激光 + 穿透效果
```

**实现**：每帧绘制线条，射线检测命中。

#### 6.4.2 散弹 (Spread)

```
等级1: 3发扇形（-15°, 0°, +15°）
等级2: 5发扇形
等级3: 5发 + 伤害提升
等级4: 7发 + 射速提升
等级5: 9发 + 两排散射（上下错开）
```

#### 6.4.3 导弹 (Missile)

```
等级1: 同时发射1枚
等级2: 同时发射2枚（左右翼）
等级3: 3枚 + 追踪速度+
等级4: 4枚 + 爆炸范围+
等级5: 6枚 + 伤害翻倍
```

**追踪算法**：每帧计算目标方向，逐步调整速度方向。

#### 6.4.4 等离子环 (Plasma)

```
等级1: 1个等离子球环绕
等级2: 2个等离子球
等级3: 3个 + 环绕半径+
等级4: 4个 + 伤害+
等级5: 6个 + 可发射能量弹
```

### 6.5 BOSS 系统

#### 6.5.1 BOSS 阶段配置结构

```javascript
const BOSS_DATA = {
  voidCarrier: {
    name: '虚空母舰',
    maxHp: 3000,
    width: 240,
    height: 180,
    phases: [
      {
        hpPercent: [1.0, 0.7],
        patterns: ['spread', 'summon'],
        patternDuration: 5000,
        moveSpeed: 1
      },
      {
        hpPercent: [0.7, 0.4],
        patterns: ['spiral', 'laser'],
        patternDuration: 4000,
        moveSpeed: 1.5
      },
      {
        hpPercent: [0.4, 0.0],
        patterns: ['bulletHell', 'charge', 'tracker'],
        patternDuration: 3000,
        moveSpeed: 2,
        enraged: true
      }
    ]
  }
};
```

#### 6.5.2 BOSS 弹幕模式实现方式

每种弹幕模式是一个**生成器函数**，接收 BOSS 实例和时间参数，输出子弹：

```javascript
const BossPatterns = {
  // 扇形弹幕
  spread(boss, dt) {
    boss.patternTimer += dt;
    if (boss.patternTimer >= 800) {
      boss.patternTimer = 0;
      const bulletCount = 7 + boss.currentPhase * 3;
      const spreadAngle = Math.PI / 3;
      for (let i = 0; i < bulletCount; i++) {
        const angle = Math.PI/2 - spreadAngle/2 + (spreadAngle / (bulletCount-1)) * i;
        spawnEnemyBullet(boss.x, boss.y + boss.height/2, angle, 3);
      }
    }
  },
  
  // 旋转弹幕
  spiral(boss, dt) {
    boss.patternTimer += dt;
    boss.spiralAngle = (boss.spiralAngle || 0) + dt * 0.005;
    if (boss.patternTimer >= 100) {
      boss.patternTimer = 0;
      for (let i = 0; i < 3; i++) {
        const angle = boss.spiralAngle + (i * Math.PI * 2 / 3);
        spawnEnemyBullet(boss.x, boss.y, angle, 4);
      }
    }
  },
  
  // 更多模式...
};
```

### 6.6 粒子系统

#### 6.6.1 粒子效果清单

| 效果 | 触发时机 | 粒子数 | 颜色 |
|------|----------|--------|------|
| 子弹拖尾 | 玩家子弹飞行 | 每帧2-3 | 青色/紫色 |
| 敌机爆炸 | 敌机被击毁 | 20-50 | 橙/黄/白 |
| BOSS爆炸 | BOSS死亡 | 200+ | 多色混色 |
| 护盾被击 | 护盾抵消伤害 | 30 | 蓝色 |
| 道具拾取 | 玩家拾取道具 | 15 | 对应道具色 |
| 引擎喷射 | 玩家飞机尾焰 | 每帧3-5 | 蓝/白 |
| 屏幕闪光 | 炸弹/技能释放 | 全屏闪白 | 白色 |
| 擦弹特效 | 子弹擦过玩家 | 5 | 红色 |

#### 6.6.2 粒子发射器（Emitter）

支持预设发射器配置：

```javascript
const ParticleEmitters = {
  explosion: {
    count: 30,
    speedMin: 2,
    speedMax: 6,
    lifeMin: 300,
    lifeMax: 800,
    sizeMin: 2,
    sizeMax: 6,
    colors: ['#ff6600', '#ffaa00', '#ffffff', '#ff3300'],
    blendMode: 'lighter',
    gravity: 0
  },
  // ...更多预设
};
```

### 6.7 关卡系统

#### 6.7.1 关卡数据结构

```javascript
const LEVELS = [
  {
    id: 1,
    name: '霓虹都市',
    background: 'city_neon',     // 背景主题
    music: 'level1_bgm',
    duration: 90000,             // 关卡时长（到BOSS出现）
    waves: [
      { time: 2000, type: 'scout', formation: 'line', count: 5, y: -50 },
      { time: 5000, type: 'scout', formation: 'v_shape', count: 7 },
      { time: 10000, type: 'fighter', formation: 'pair', count: 4 },
      { time: 15000, type: 'scout', formation: 'rain', count: 12 },
      { time: 25000, type: 'bomber', formation: 'single', count: 2 },
      // ... 更多波次
    ],
    boss: 'voidCarrier',          // BOSS ID
    bossEntryTime: 90000          // BOSS 出场时间
  },
  // ...更多关卡
];
```

#### 6.7.2 敌潮编队类型

| 编队 | 描述 | 示意 |
|------|------|------|
| `line` | 横向一字排开 | `→ → → → →` |
| `v_shape` | V字编队 | `  →  \n → → → \n→   →` |
| `pair` | 双机并排 | `→ →` |
| `rain` | 雨点式，随机X下落 | 散布全屏 |
| `circle` | 圆形包围 | 围向玩家 |
| `dive` | 俯冲编队 | 上方冲入，下方撤出 |

### 6.8 背景滚动系统

**视差滚动（Parallax Scrolling）**：多层背景以不同速度滚动，营造深度感。

```
共5层背景，从远到近：
Layer 0: 星空 (速度 0.1x) - 最远处
Layer 1: 远景建筑轮廓 (速度 0.3x)
Layer 2: 中景建筑 (速度 0.5x)
Layer 3: 近景建筑/管道 (速度 0.8x)
Layer 4: 前景雾气/粒子 (速度 1.0x) - 最前方
```

**科技风格背景元素**：
- 霓虹线条、网格地面
- 数据流、数字雨
- 扫描线、故障效果（Glitch）
- 远处的城市天际线

---

## 7. UI/UX 设计规范

### 7.1 视觉风格定义

#### 7.1.1 色彩方案（赛博朋克科技风）

| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 主色调（青） | `#00f0ff` | 玩家、UI 主色 |
| 强调色（紫） | `#bf00ff` | 能量、升级 |
| 警示色（红） | `#ff0066` | 危险、敌方、掉血 |
| 成功色（绿） | `#00ff88` | 回血、通过 |
| 金色 | `#ffcc00` | 分数、奖励 |
| 背景深 | `#0a0a1a` | 画布背景 |
| 背景中 | `#1a1a3a` | UI 背景 |
| 边框 | `#2a2a5a` | 分隔线、边框 |

#### 7.1.2 字体

- **主字体**：`'Orbitron', 'Rajdhani', sans-serif`（科技感字体，Google Fonts）
- **备选**：`'Courier New', monospace`
- **数字/分数**：等宽字体，加粗

#### 7.1.3 视觉效果

| 效果 | 应用场景 |
|------|----------|
| 外发光 (text-shadow / box-shadow) | 标题、按钮、HUD 文字 |
| 扫描线 (scanline) | 全屏叠加，模拟 CRT 显示器 |
| 故障效果 (Glitch) | BOSS 出现、受伤、标题动画 |
| 辉光 (Bloom) | 子弹、爆炸、激光（lighter 混合模式） |
| 渐隐 (Fade) | 界面切换、弹入弹出 |
| 屏幕震动 | BOSS 撞击、爆炸、受伤 |

### 7.2 HUD（平视显示器）布局

```
┌─────────────────────────────────────────────┐
│ [生命] ♥♥♥    [分数] 012,345    [炸弹] 💣💣 │ ← 顶部栏
│ [武器] 激光 Lv.3    [能量] ████████░░ 78%   │ ← 第二行
│                                             │
│                                             │
│                  游戏区域                    │
│                                             │
│                                             │
│ [连击] x15 COMBO!              [关卡] 1-1   │ ← 底部栏
└─────────────────────────────────────────────┘
```

**HUD 元素说明**：
- **左上角**：生命值（心形图标 × 数量）
- **中上**：分数（6位数，逗号分隔）
- **右上**：炸弹数量
- **左中**：当前武器名称 + 等级
- **武器下方**：必杀能量条
- **左下**：连击数（超过 5 连击时显示）
- **右下**：当前关卡编号

### 7.3 界面设计

#### 7.3.1 主菜单

```
╔══════════════════════════════╗
║                              ║
║     ▓▓▓ 飞机大战 ▓▓▓        ║  ← 游戏标题（霓虹发光+故障效果）
║     CYBER FIGHTER           ║
║                              ║
║        ▶ 开始游戏            ║  ← 选中项有辉光动画
║          关卡选择            ║
║          战机选择            ║
║          排行榜              ║
║          设置                ║
║                              ║
║   最高分: 1,234,567          ║
║                              ║
╚══════════════════════════════╝
```

#### 7.3.2 暂停界面

半透明黑色遮罩 + 居中菜单面板：
- 继续游戏
- 重新开始
- 返回主菜单

#### 7.3.3 结算界面

```
╔══════════════════════════════╗
║         STAGE CLEAR!         ║  ← 大字胜利/失败
║                              ║
║   最终分数:   1,234,567      ║
║   最高连击:   x45            ║
║   击毁敌机:   128            ║
║   获得道具:   15             ║
║   用时:       02:34          ║
║                              ║
║   历史最高:   2,000,000      ║
║   评级:       S              ║  ← S/A/B/C/D
║                              ║
║      [再玩一次] [下一关]     ║
║      [返回菜单]              ║
╚══════════════════════════════╝
```

**评级标准**（参考）：
| 评级 | 分数比例 | 条件 |
|------|----------|------|
| S | ≥90% | 不续关 + 低受伤 |
| A | ≥70% | |
| B | ≥50% | |
| C | ≥30% | |
| D | <30% | 通关即可 |

### 7.4 动效设计

| 元素 | 进入动画 | 交互动画 |
|------|----------|----------|
| 按钮 | 从下往上滑入 + 渐显 | 悬停放大+发光，点击缩放 |
| 面板 | 缩放 0.9→1 + 渐显 | |
| 分数弹出 | 向上飘移 + 渐隐 | |
| 连击提示 | 缩放弹跳 | 数字跳动 |
| BOSS警告 | 全屏闪红 + "WARNING" 大字 | 警报声 |
| 道具掉落 | 旋转下落 | 拾取时扩散圈 |

### 7.5 响应式设计

- **画布尺寸**：逻辑分辨率 480×800（纵向）
- **适配方式**：保持宽高比，居中显示，两侧留黑边或背景
- **移动端**：Canvas 占满屏幕，触控操作区全屏幕
- **桌面端**：固定尺寸或可缩放窗口

---

## 8. 关卡与进度设计

### 8.1 关卡规划（5 大关）

| 关卡 | 名称 | 主题 | BOSS | 新敌机 |
|------|------|------|------|--------|
| 1 | 霓虹都市 | 城市夜景、霓虹灯光 | 虚空母舰 | 小型机、中型机 |
| 2 | 数据要塞 | 数字空间、数据流 | 防火墙守卫 | 重型机、追踪弹 |
| 3 | 虫洞深渊 | 宇宙、星云、陨石 | 虚空吞噬者 | 精英机、激光 |
| 4 | 机械工厂 | 工厂、管道、齿轮 | 锻造之神 | 全部类型 + 新弹幕 |
| 5 | 核心禁区 | BOSS 老巢、终极战场 | 终焉之主 | BOSS 多形态 |

### 8.2 难度曲线

```
难度
  ↑
  │        ╱BOSS
  │       ╱
  │    ╱╱ 精英机出现
  │   ╱
  │ ╱╱ 中型机增多
  │╱
  ┼──────────────────→ 时间
  开始              BOSS战
```

**难度递增手段**：
1. 敌机数量增加
2. 敌机血量提升
3. 射击频率加快
4. 弹幕密度提升
5. 移动速度加快
6. 新敌机类型引入

### 8.3 进度保存

**保存内容**（localStorage）：
- 最高分数
- 已解锁关卡
- 最高连击记录
- 各关最高评级
- 设置项（音量、难度等）

---

## 9. 性能优化方案

### 9.1 渲染优化

1. **离屏 Canvas 缓存**：静态背景层预渲染到离屏 Canvas，每帧直接贴图
2. **对象池**：子弹、粒子、敌机全部复用，避免 GC
3. **减少绘制调用**：同类型粒子批量绘制（同颜色/同混合模式）
4. **视锥剔除**：屏幕外的对象跳过渲染（逻辑仍更新，但不绘制）
5. **分层渲染**：背景层、游戏层、UI 层分离，只有变化的层重绘

### 9.2 逻辑优化

1. **空间网格划分**：碰撞检测从 O(n²) 降到近似 O(n)
2. **子弹上限**：敌方子弹最多 300 发，超出则淘汰最早的
3. **粒子上限**：最多 500 个粒子，超出复用最老的
4. **AI 计算节流**：非关键敌机每 2-3 帧更新一次 AI
5. **距离平方比较**：碰撞检测用平方距离，避免开方运算

### 9.3 移动端优化

1. **DPR 适配**：Canvas 实际像素 = CSS 像素 × devicePixelRatio，但限制最大 2x
2. **降低粒子数**：移动端粒子数减半
3. **降低弹幕密度**：移动端难度自动微调
4. **禁用阴影**：text-shadow / box-shadow 消耗大，移动端简化
5. **触摸优化**：touch-action: none，禁止页面滚动和缩放

### 9.4 性能监控

- 内置 FPS 显示（调试模式）
- 内存使用监控（Chrome DevTools）
- 关键路径性能埋点

---

## 10. 实现步骤与里程碑

### 10.1 开发路线图

#### 阶段 1：基础框架（第 1-2 天）

- [ ] 项目文件结构搭建
- [ ] Canvas 初始化 + 自适应
- [ ] 游戏主循环 + 状态机
- [ ] 输入管理（鼠标/键盘）
- [ ] 对象池工具类
- [ ] 粒子系统基础
- [ ] 音效管理框架（可选，先用占位）

**里程碑**：空画布能跑 60FPS，能切换状态

#### 阶段 2：玩家与基础射击（第 3-4 天）

- [ ] 玩家飞机绘制（矩形/简单图形先占位）
- [ ] 玩家移动控制（鼠标+键盘）
- [ ] 子弹系统（玩家单发）
- [ ] 碰撞检测基础
- [ ] 玩家生命值 + 无敌帧
- [ ] 单层背景滚动

**里程碑**：能动、能射、有背景

#### 阶段 3：敌机与道具（第 5-7 天）

- [ ] 敌机基础类 + 3 种敌机
- [ ] 敌机生成波次
- [ ] 敌方子弹
- [ ] 道具系统（5 种道具）
- [ ] 武器系统（2 种武器先做）
- [ ] 得分系统 + HUD
- [ ] 粒子爆炸效果

**里程碑**：有完整打飞机体验

#### 阶段 4：BOSS 战（第 8-10 天）

- [ ] BOSS 基类
- [ ] 多阶段切换
- [ ] 5 种弹幕模式
- [ ] BOSS 入场/退场动画
- [ ] BOSS UI 血条
- [ ] 屏幕震动 + 演出效果

**里程碑**：第一关完整可通关

#### 阶段 5：完善玩法（第 11-14 天）

- [ ] 剩余 3 种武器
- [ ] 炸弹/清屏技能
- [ ] 能量条 + 必杀技
- [ ] 连击系统
- [ ] 擦弹系统
- [ ] 5 层视差滚动背景
- [ ] 科技风格背景元素

**里程碑**：玩法丰富度达标

#### 阶段 6：UI 与美术（第 15-18 天）

- [ ] 主菜单界面
- [ ] 关卡选择
- [ ] 暂停界面
- [ ] 结算界面
- [ ] 设置界面
- [ ] 所有界面科技风格美化
- [ ] 字体 + 发光效果
- [ ] 扫描线 / 故障效果

**里程碑**：UI 完整，风格统一

#### 阶段 7：内容扩充 + 打磨（第 19-25 天）

- [ ] 3-5 个关卡
- [ ] 3 个 BOSS
- [ ] 难度选择
- [ ] 排行榜 / 存档
- [ ] 音效 + 背景音乐
- [ ] 移动端触控支持
- [ ] 性能优化
- [ ] Bug 修复 + 平衡性调整

**里程碑**：可发布版本

### 10.2 文件结构

```
飞机大战/
├── index.html              # 主 HTML 文件
├── css/
│   └── style.css           # 样式表
├── js/
│   ├── main.js             # 入口文件
│   ├── game.js             # 游戏核心
│   ├── player.js           # 玩家
│   ├── enemy.js            # 敌机
│   ├── boss.js             # BOSS
│   ├── bullet.js           # 子弹管理
│   ├── powerup.js          # 道具
│   ├── particle.js         # 粒子系统
│   ├── collision.js        # 碰撞检测
│   ├── level.js            # 关卡管理
│   ├── renderer.js         # 渲染器
│   ├── input.js            # 输入管理
│   ├── audio.js            # 音频管理
│   ├── ui.js               # UI 管理
│   └── utils.js            # 工具函数
├── assets/                 # 图片/音效资源（可选）
│   ├── images/
│   └── audio/
└── 飞机大战技术设计文档.md
```

---

## 11. 测试验收标准

### 11.1 功能验收

| 模块 | 验收项 | 通过标准 |
|------|--------|----------|
| 玩家控制 | 鼠标/键盘移动 | 流畅跟随，无延迟、无卡顿 |
| 射击 | 武器切换与升级 | 5种武器，每种5级，效果正确 |
| 敌机 | 4种敌机行为 | AI 正常，移动模式正确 |
| BOSS | 多阶段弹幕 | 阶段切换正常，弹幕模式正确 |
| 道具 | 5种道具效果 | 拾取正确，效果正确，持续时间正确 |
| 碰撞 | 各类碰撞判定 | 无漏判、无误判，判定点合理 |
| 关卡 | 多关卡切换 | 关卡进度正常，BOSS 触发正确 |
| UI | 全部界面 | 各界面正常跳转，数据显示正确 |
| 存档 | localStorage | 最高分、解锁进度正确保存和读取 |
| 音效 | 背景音乐+音效 | 不卡顿，可调节音量 |

### 11.2 性能验收

| 指标 | 桌面端 | 移动端 |
|------|--------|--------|
| 帧率 | ≥55 FPS | ≥25 FPS |
| 加载时间 | ≤2s | ≤3s |
| 内存 | ≤150MB | ≤200MB |
| 发热（移动） | - | 连续30分钟不烫手 |

**测试设备参考**：
- 桌面：Chrome 最新版，1080p
- 移动：iPhone 12 / 小米 10 同档及以上

### 11.3 兼容性验收

- [ ] Chrome 90+
- [ ] Firefox 88+
- [ ] Safari 14+
- [ ] Edge 90+
- [ ] iOS Safari 14+
- [ ] Android Chrome 90+

### 11.4 体验验收

- [ ] 打击感：命中反馈明确，爆炸有冲击力
- [ ] 节奏：敌潮疏密有致，不无聊也不劝退
- [ ] 难度：NORMAL 难度普通玩家能通关第一关
- [ ] 视觉：科技风格统一，画面不杂乱
- [ ] 操作：跟手、精准、无误触

---

## 附录

### A. 代码风格约定

- 使用 ES6 Class 语法
- 2 空格缩进
- 变量名 camelCase，类名 PascalCase
- 常量全大写 + 下划线
- 适度注释，解释「为什么」而非「是什么」
- 单文件不超过 500 行（超出则拆分）

### B. 调试技巧

1. **无敌模式**：按 `I` 键切换无敌（调试用）
2. **跳关卡**：按 `L` 键跳到下一关
3. **加炸弹**：按 `B` 键增加炸弹
4. **显示判定点**：按 `H` 键显示碰撞盒
5. **显示 FPS**：按 `F` 键切换 FPS 显示
6. **清屏**：按 `K` 键消灭所有敌机（BOSS除外）

### C. 数学工具速查

```javascript
// 常用数学函数，放在 utils.js 中
const Utils = {
  random(min, max) { return Math.random() * (max - min) + min; },
  randomInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; },
  dist(x1, y1, x2, y2) { return Math.sqrt((x2-x1)**2 + (y2-y1)**2); },
  distSq(x1, y1, x2, y2) { return (x2-x1)**2 + (y2-y1)**2; },  // 优先用这个
  angle(x1, y1, x2, y2) { return Math.atan2(y2-y1, x2-x1); },
  lerp(a, b, t) { return a + (b - a) * t; },
  clamp(val, min, max) { return Math.max(min, Math.min(max, val)); },
};
```

---

> **文档结束**  
> 本设计文档为 Solo Builder 提供完整的技术实现蓝图。建议按照「实现步骤与里程碑」章节逐步开发，每完成一个里程碑都运行测试，确保质量。  
> 如有疑问，可随时调整设计，灵活应对开发中的实际问题。
